package com.example.uzhavan.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary greens ────────────────────────────────────────────────────────────
val AgriGreen        = Color(0xFF1A6B3C)   // deep forest green — primary action
val AgriGreenLight   = Color(0xFF2E9E5B)   // hover / ripple
val AgriGreenDark    = Color(0xFF0F4226)   // headings / emphasis
val AgriGreenSurface = Color(0xFFEDF7F1)   // chip / tag backgrounds

// ── Surfaces & backgrounds ────────────────────────────────────────────────────
val AgriBackground   = Color(0xFFF7F7F5)   // warm off-white — like Notion/Linear
val AgriSurface      = Color(0xFFFFFFFF)
val AgriWhite        = Color(0xFFFFFFFF)

// ── Neutrals ──────────────────────────────────────────────────────────────────
val AgriGray         = Color(0xFF8A8A8A)   // secondary text
val AgriGrayLight    = Color(0xFFB0B0B0)   // placeholder text
val AgriDivider      = Color(0xFFEEEEEE)   // subtle dividers

// ── Accent ────────────────────────────────────────────────────────────────────
val AgriYellow       = Color(0xFFF59E0B)   // amber — badges / highlights
val AgriYellowLight  = Color(0xFFFEF3C7)   // amber surface

// ── Earth tones ───────────────────────────────────────────────────────────────
val AgriEarth        = Color(0xFF7C5C3E)
val AgriEarthLight   = Color(0xFFA07850)

// ── Semantic ──────────────────────────────────────────────────────────────────
val AgriOnSurface    = Color(0xFF0F4226)
val AgriError        = Color(0xFFDC2626)
